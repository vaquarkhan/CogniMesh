# CogniMesh - pull published images from GHCR (no local build)
# Usage: docker compose -f docker-compose.prod.yml up
# Portal: http://localhost:3000  API: http://localhost:4000  Catalog: http://localhost:8080

services:
  catalog:
    image: ghcr.io/vaquarkhan/cognimesh-catalog:1.0.0
    ports:
      - "8080:8080"
    environment:
      SPRING_APPLICATION_NAME: cognimesh-catalog
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/api/v1/products"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s

  api:
    image: ghcr.io/vaquarkhan/cognimesh-api:1.0.0
    ports:
      - "4000:4000"
    environment:
      # Local pull-and-run demo only. Use Cognito + NODE_ENV=production for real deploys.
      NODE_ENV: development
      AUTH_DISABLED: "true"
      PORT: "4000"
      CATALOG_URL: http://catalog:8080
      CATALOG_FALLBACK: embedded
      CORS_ORIGINS: http://localhost:3000
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:4000/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 5s
      retries: 18
      start_period: 15s
    depends_on:
      catalog:
        condition: service_healthy

  portal:
    image: ghcr.io/vaquarkhan/cognimesh-portal:1.0.0
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy
